class Solution {
    public int addRungs(final int[] rungs, final int dist) {
        int curr = 0, added = 0;

        for(final int rung : rungs) {
            final int d = rung - curr;

            added += (d - 1) / dist;

            curr = rung;
        }

        return added;
    }
}
